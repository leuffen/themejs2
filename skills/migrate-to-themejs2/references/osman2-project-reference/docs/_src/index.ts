import "@trunkjs/loader";
import "./style.scss";

// Aktiviert die Formmailer-Erweiterung nur für diese Website und hält das Theme selbst optional.
// Konfiguration von Endpoint und Subscription-Key erfolgt über die 10_blanc-Layout-Variablen `micx_endpoint` und `micx_subscription`.
import "@leuffen/themejs2/functions/formmailer";

import "@leuffen/themejs2";
